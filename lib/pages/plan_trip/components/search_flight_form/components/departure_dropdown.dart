import 'package:flutter/material.dart';

class DepartureDropdown extends StatelessWidget{
  const DepartureDropdown({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('DepartureDropdown'),
    );
  }
}